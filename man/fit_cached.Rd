% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-cache.R
\name{fit_cached}
\alias{fit_cached}
\title{Cached model fit}
\usage{
fit_cached(responses, config, cache = NULL, ...)
}
\arguments{
\item{responses}{A \code{response_data} object.}

\item{config}{A \code{model_config} object.}

\item{cache}{A \code{cachem} cache (e.g. from \code{\link[=fit_cache]{fit_cache()}}) or \code{NULL}.}

\item{...}{Arguments passed to \code{\link[=fit_model]{fit_model()}} (e.g. \code{chains},
\code{iter_warmup}, \code{iter_sampling}, \code{adapt_delta}, \code{max_treedepth}).}
}
\value{
A \code{fit_result} object.
}
\description{
Wraps \code{\link[=fit_model]{fit_model()}} with optional disk-backed caching keyed on a hash of
the config, the response data, and the fit settings. With \code{cache = NULL}
(the default) behaviour is identical to \code{\link[=fit_model]{fit_model()}}.
}
\details{
Use cases: resumability across sessions, replay during development, and
duplicate-config deduplication when batching multiple proposals or
running structure-search algorithms over a population of particles.
}
\seealso{
\code{\link[=fit_cache]{fit_cache()}}, \code{\link[=fit_many]{fit_many()}}
}
