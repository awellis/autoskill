% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-problem.R
\name{causal_problem}
\alias{causal_problem}
\title{Construct a structure_problem for causal DAG discovery}
\usage{
causal_problem(data, variables = NULL)
}
\arguments{
\item{data}{A data frame of continuous observations. Column names are
the variable names.}

\item{variables}{Optional subset of \code{names(data)} to include. Defaults
to all numeric columns.}
}
\value{
A \code{structure_problem} of subclass \code{"causal_problem"}.
}
\description{
A second application of the \code{\link[=structure_problem]{structure_problem()}} interface, built to
validate the abstraction. Uses adjacency-matrix DAGs over named
variables, scores via per-node Gaussian regression with BIC, and
proposes refinements via random local moves.
}
\details{
Intentionally minimal: no LLM proposer yet (the random kernel is
enough to validate that the loop, caching, and stacking machinery work
for non-skill applications). An LLM-driven proposer is a natural
follow-up that slots into the same \code{propose_refinement} slot.
}
\seealso{
\code{\link[=structure_problem]{structure_problem()}}, \code{\link[=optimize_structure]{optimize_structure()}},
\code{\link[=score_causal_dag]{score_causal_dag()}}
}
