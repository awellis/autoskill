% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure-optimizer.R
\name{optimize_structure_skill}
\alias{optimize_structure_skill}
\title{Skill-flavoured legacy wrapper around \code{\link[=optimize_structure]{optimize_structure()}}}
\usage{
optimize_structure_skill(
  responses,
  items,
  initial_config = NULL,
  edge_prior = NULL,
  chat = NULL,
  model = "claude-sonnet-4-20250514",
  interactive = FALSE,
  max_iter = 10,
  patience = 3,
  cache = NULL,
  log_file = NULL,
  ...
)
}
\arguments{
\item{responses}{A \code{response_data} object.}

\item{items}{Tibble with \code{item_id} and \code{text}.}

\item{initial_config}{Optional \code{model_config} to start from. If \code{NULL},
the LLM proposer runs.}

\item{edge_prior}{Optional \code{edge_prior} object.}

\item{chat}{An \code{ellmer} chat object.}

\item{model}{LLM model identifier (used when \code{chat} is \code{NULL}).}

\item{interactive}{If \code{TRUE}, prompts to accept/reject each refinement.}

\item{max_iter, patience, cache, log_file}{See \code{\link[=optimize_structure]{optimize_structure()}}.}

\item{...}{Passed to \code{problem$score()} (i.e. fit_cached / fit_model).}
}
\value{
An S3 object of class \code{optimization_result}.
}
\description{
Preserves the pre-refactor calling convention: takes responses, items,
optional initial config and chat. Internally constructs a
\code{\link[=skill_problem]{skill_problem()}} and forwards to the generic \code{\link[=optimize_structure]{optimize_structure()}}.
}
\details{
New code should prefer the generic form
(\code{optimize_structure(skill_problem(...))}); this wrapper exists so
existing examples and downstream callers keep working unchanged.
}
