% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skill-problem.R
\name{skill_problem}
\alias{skill_problem}
\title{Construct a structure_problem for skill discovery}
\usage{
skill_problem(
  items,
  responses,
  edge_prior = NULL,
  chat = NULL,
  model = "claude-sonnet-4-20250514",
  interactive = FALSE,
  local_move_kernel = c("random", "llm")
)
}
\arguments{
\item{items}{Tibble with columns \code{item_id} and \code{text}.}

\item{responses}{A \code{response_data} object.}

\item{edge_prior}{Optional \code{edge_prior} object (required for
\code{structural = "dag"}).}

\item{chat}{An \code{ellmer} chat object. If \code{NULL}, one is created with
the standard reflection system prompt.}

\item{model}{LLM model identifier (used only when \code{chat} is \code{NULL}).}

\item{interactive}{If \code{TRUE}, prompts the user to accept/reject each
LLM-proposed refinement.}

\item{local_move_kernel}{SMC mutation kernel. \code{"random"} (default)
uses \code{\link[=propose_skill_local_move]{propose_skill_local_move()}}: random Lambda flip or block swap.
\code{"llm"} uses \code{\link[=propose_skill_local_move_llm]{propose_skill_local_move_llm()}} which asks the LLM
for a semantically grounded move (toggle one loading or merge two
skills). The LLM kernel is much slower but produces better-quality
moves; tradeoff is wall-clock time vs SMC mixing efficiency.}
}
\value{
A \code{structure_problem} of subclass \code{"skill_problem"}.
}
\description{
Wraps the existing skill workflow (\code{propose_skills()},
\code{propose_refinement()}, \code{model_config()}, \code{check_identifiability()},
\code{fit_cached()}) into a \code{\link[=structure_problem]{structure_problem()}} that the generic
optimisation loop can drive. The constructor binds an \code{ellmer} chat
object and the problem's data into closures over each slot.
}
\details{
This is the primary application of the generic engine. Other
applications (causal discovery, mediation, path analysis) follow the
same pattern but with different proposers and scorers.
}
\seealso{
\code{\link[=structure_problem]{structure_problem()}}, \code{\link[=optimize_structure]{optimize_structure()}}
}
