% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure-problem.R
\name{structure_problem}
\alias{structure_problem}
\title{Construct a structure_problem}
\usage{
structure_problem(
  data,
  propose_initial,
  propose_refinement,
  score,
  log_prior,
  validate,
  cache_key,
  summarize_structure,
  summarize_fit,
  propose_local_move = NULL,
  class = character(0)
)
}
\arguments{
\item{data}{Anything. Typically a list of the data the problem
operates over. Bound for reporting and for closures to reference.}

\item{propose_initial}{\verb{function(...) -> structure}. Returns the
starting structure for the search.}

\item{propose_refinement}{\verb{function(current, fit_result, history) -> structure | NULL}. Given a fit and the run history, returns the
next structure to evaluate. May return \code{NULL} to signal "no
refinement, keep current".}

\item{score}{\verb{function(structure, cache, ...) -> fit_result}. Fits
the model implied by the structure and returns a \code{fit_result}-shaped
object with at minimum a \verb{$loo} field.}

\item{log_prior}{\verb{function(structure) -> numeric}. Log probability of
the structure under the problem's prior.}

\item{validate}{\verb{function(structure) -> list(passed, problems)}.
Cheap pre-fit check (identifiability, acyclicity, etc.).}

\item{cache_key}{\verb{function(structure, fit_args) -> character}. Hash
identifying the (structure, data, fit-settings) triple for caching.}

\item{summarize_structure}{\verb{function(structure) -> character}. Short
human-readable label, used in logs and progress reports.}

\item{summarize_fit}{\verb{function(fit_result) -> list}. Domain-relevant
summary of a fit, fed to the LLM for reflection.}

\item{propose_local_move}{Optional. \verb{function(structure, move_type, ...) -> structure}. Required only by SMC; greedy search ignores it.}

\item{class}{Additional S3 classes to prepend to \code{"structure_problem"}.
Domain constructors set this (e.g. \code{class = "skill_problem"}).}
}
\value{
An S3 object inheriting from \code{"structure_problem"}.
}
\description{
A \code{structure_problem} is the interface between a domain (skills,
causal discovery, mediation, etc.) and the generic optimisation
machinery (the optimization loop, future SMC). It bundles all the
domain-specific concerns into a single object the loop can drive
uniformly.
}
\details{
Each slot is a closure typically built by a domain-specific
constructor such as \code{\link[=skill_problem]{skill_problem()}}. The optimisation loop only
interacts with these slots: it never imports domain-specific code
directly.
}
\seealso{
\code{\link[=is_structure_problem]{is_structure_problem()}}
}
