% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-problem.R
\name{random_local_move}
\alias{random_local_move}
\title{Propose a single random local move on a DAG}
\usage{
random_local_move(dag, max_attempts = 50L)
}
\arguments{
\item{dag}{A \code{causal_dag}.}

\item{max_attempts}{Maximum proposals to try before giving up.}
}
\value{
A new \code{causal_dag}. Returns the input unchanged if no valid
move was found.
}
\description{
Picks one of \code{add_edge}, \code{remove_edge}, \code{reverse_edge} at random and
applies it, retrying up to \code{max_attempts} times if the result would
create a cycle. Used as the placeholder mutation kernel for
\code{causal_problem()} until an LLM-driven proposer lands.
}
