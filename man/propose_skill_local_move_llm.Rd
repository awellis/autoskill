% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skill-moves-llm.R
\name{propose_skill_local_move_llm}
\alias{propose_skill_local_move_llm}
\title{Propose a skill local move via LLM}
\usage{
propose_skill_local_move_llm(config, chat, items)
}
\arguments{
\item{config}{A \code{model_config}.}

\item{chat}{An \code{ellmer} chat object.}

\item{items}{Tibble with \code{item_id} and \code{text} columns. Used to give
the LLM the item text for context.}
}
\value{
A new \code{model_config}, or \code{config} unchanged on any failure.
}
\description{
Asks the LLM to suggest a single small change to the current model
configuration: toggle one loading, merge two conceptually overlapping
skills, or decline (no-op). The LLM sees the item text, the current
skill taxonomy, and which skills each item currently loads on.
}
\details{
Move types:
\itemize{
\item \code{flip_loading}: Set \code{Lambda[item, skill]} from 0 to 1 or 1 to 0.
Useful for adding a missing semantic dependency or removing a
spurious one.
\item \code{merge_skills}: Combine two skills that conceptually overlap. The
merged skill takes the union of items from both. Drops one skill ID
and any edges in the prior involving it.
\item \code{no_op}: Return the structure unchanged (LLM declines).
}

Failed or invalid moves (LLM error, unknown item or skill ID, move
that breaks identifiability) return the input config unchanged. Valid
to use as a \code{propose_local_move} slot on a \code{\link[=skill_problem]{skill_problem()}}.
}
\seealso{
\code{\link[=propose_skill_local_move]{propose_skill_local_move()}}, \code{\link[=skill_problem]{skill_problem()}}
}
