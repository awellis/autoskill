% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skill-moves.R
\name{propose_skill_local_move}
\alias{propose_skill_local_move}
\title{Propose a single local move on a skill model_config}
\usage{
propose_skill_local_move(config, ...)
}
\arguments{
\item{config}{A \code{model_config}.}

\item{...}{Unused; present for compatibility with the structure_problem
slot signature.}
}
\value{
A new \code{model_config}, or \code{config} unchanged if the move would
produce an invalid structure.
}
\description{
Stochastic mutation kernel for SMC over skill structures. Picks one of
two move types and applies it: with probability 0.7, flips a single
entry of the loading mask; with probability 0.3, swaps one block
option (measurement, item, or link). Moves that would create
non-identifiable structures (orphan items, skills with fewer than two
items) are rejected and the input is returned unchanged.
}
\details{
Wired into \code{\link[=skill_problem]{skill_problem()}} as the \code{propose_local_move} slot, so
\code{\link[=optimize_structure_smc]{optimize_structure_smc()}} can drive skill_problems just as it drives
causal_problems.
}
