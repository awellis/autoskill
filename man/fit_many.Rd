% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-parallel.R
\name{fit_many}
\alias{fit_many}
\title{Fit multiple configs in parallel}
\usage{
fit_many(responses, configs, cache = NULL, ...)
}
\arguments{
\item{responses}{A \code{response_data} object.}

\item{configs}{A list of \code{model_config} objects.}

\item{cache}{A \code{cachem} cache (optional). When sharing across workers,
construct with an absolute path.}

\item{...}{Arguments passed to \code{\link[=fit_cached]{fit_cached()}}.}
}
\value{
A list the same length as \code{configs}. Each element is either a
\code{fit_result} (success) or a \code{fit_error} (failure).
}
\description{
Runs \code{\link[=fit_cached]{fit_cached()}} over a list of configs using \code{furrr::future_map()}.
Parallelism is controlled by the user via \code{future::plan()}; with the
default sequential plan, fits run serially. Cache hits are served
without invoking Stan, so duplicate configs in the batch are deduped.
}
\details{
Errors in a single fit do not abort the batch: failed configs return a
\code{fit_error} object with the underlying error message. Filter the result
with \code{purrr::keep(results, inherits, "fit_result")}.

To enable parallel execution:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{future::plan(future::multisession, workers = 4)
results <- fit_many(responses, configs, cache = fit_cache())
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=fit_cached]{fit_cached()}}, \code{\link[=fit_cache]{fit_cache()}}
}
